<h2 class="fw-bold mb-5 text-uppercase">
    Upgrade Status To <span class="text-danger">Admin</span>
</h2>

<ul class="list-group card-body ms-3">
    <li class="list-group-item">You will be able to delete posts and users.</li>
</ul>

<form class="card-body my-5 ms-3" action="/upgrade" method="POST">
    <div class="mb-md-1 mt-md-1 pb-5 pt-2">
        <div data-mdb-input-init class="form-outline form-white mb-4">
            <p class="ms-3">
                To become an <strong class="text-danger">admin</strong>, send a
                request. We will respond as soon as possible.
            </p>
            <% if (user.adminrequest) { %>
            <div class="text-success ms-3">
                <strong>You've sent a request. We are processing it.</strong>
            </div>
            <% } %>
        </div>

        <button <% if (user.adminrequest) { %>
            disabled <% } %> class="btn btn-outline-success px-5"
            type="submit">Send
        </button>
    </div>
</form>
